{
  "name": "finance-calculator-backend",
  "private": true,
  "scripts": {
    "build": "npm --prefix .. run backend:build",
    "dev": "npm --prefix .. run backend:dev",
    "start": "npm --prefix .. run backend:start"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "multer": "^2.1.1",
    "multer-storage-cloudinary": "^4.0.0"
  }
}
